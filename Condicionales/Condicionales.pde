//Iniciar
int distancia=1;

void setup() {
  size(500, 500);

void draw(){
  if (mouseX > 200) {
   ellipse(mouseX, mouseY,50,50);
  } else if { if (mouseX > 300){
    ellipse(mouseX, mouseY,10,10);
  }else{
    println(;
  
  }
}
